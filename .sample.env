# Sample environment variables for sip-bridge
#
# Copy this file to `.env` and adjust values as needed.

# ntfy server configuration
NTFY_URL=https://ntfy.sh
NTFY_TOPIC=my-alerts
# NTFY_AUTH=user:pass

# Webhook configuration (host, port and path must all be set for webhook to work)
# Example:
# WEBHOOK_HOST=example.com
# WEBHOOK_PORT=80
# WEBHOOK_PATH=/hook
WEBHOOK_HOST=
WEBHOOK_PORT=
WEBHOOK_PATH=

# Asterisk/FreePBX AMI settings
AMI_HOST=192.168.1.50
AMI_PORT=5038
AMI_USER=ntfybridge
AMI_PASS=SuperSecretPassword

# SIP extension to ring (line 0)
EXTENSION=1000
CHANNEL_TECH=PJSIP
DIAL_STRING=PJSIP/1000

# Context to originate from (usually 'from-internal')
CONTEXT=from-internal

# Priority within the context (normally 1)
PRIORITY=1

# Caller ID presented on the phone when alerting
CALLERID=NTFY Bridge <7777>

# Ring timeout in milliseconds
TIMEOUT_MS=30000

# Logging level for the bridge (e.g. INFO, DEBUG)
LOG_LEVEL=INFO